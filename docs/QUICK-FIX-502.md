# ⚡ Быстрое исправление 502 Bad Gateway

## 🔍 Быстрая диагностика (выполните на сервере)

```bash
# 1. Проверьте статус контейнеров
docker compose ps

# 2. Проверьте логи backend (последние 50 строк)
docker compose logs --tail=50 backend

# 3. Проверьте использование ресурсов
docker stats --no-stream

# 4. Проверьте health check
curl http://localhost:3000/health
```

## 🛠️ Быстрое решение

### Если контейнер в статусе "Restarting":

```bash
# Остановите все контейнеры
docker compose down

# Пересоберите и запустите заново
docker compose build backend
docker compose up -d

# Подождите 30 секунд и проверьте статус
sleep 30
docker compose ps
```

### Если не хватает памяти:

```bash
# Проверьте использование памяти
free -h

# Если памяти < 500MB свободно, освободите место:
# - Удалите неиспользуемые Docker образы
docker system prune -a

# - Очистите логи
docker compose logs --tail=0 backend > /dev/null 2>&1
```

### Если проблема в Nginx:

Проверьте конфигурацию Nginx и увеличьте таймауты:

```nginx
proxy_connect_timeout 60s;
proxy_send_timeout 60s;
proxy_read_timeout 60s;
```

Затем перезагрузите Nginx:
```bash
sudo nginx -t
sudo systemctl reload nginx
```

## 📊 Проверка после исправления

```bash
# 1. Проверьте статус
docker compose ps

# 2. Проверьте health check
curl http://localhost:3000/health

# 3. Проверьте в браузере
curl -I https://your-domain.com
```

## ⚠️ Если проблема повторяется

1. **Проверьте логи на ошибки:**
   ```bash
   docker compose logs backend | grep -i "error\|fatal\|exception"
   ```

2. **Проверьте подключение к БД:**
   ```bash
   docker compose exec backend npm run migrate
   ```

3. **Увеличьте ресурсы сервера** (если возможно):
   - Минимум: 1GB RAM, 1 CPU core
   - Рекомендуется: 2GB RAM, 2 CPU cores

4. **См. подробную документацию:** `docs/TROUBLESHOOTING-502.md`

---

**Важно:** После применения изменений из этого коммита, backend будет более устойчив к ошибкам и автоматически переподключаться к БД при сбоях.
