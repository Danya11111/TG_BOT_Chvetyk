# 🔧 Диагностика и исправление ошибки 502 Bad Gateway

## Причины ошибки 502

Ошибка **502 Bad Gateway** означает, что Nginx (веб-сервер) не может получить ответ от backend приложения. Это может быть вызвано:

1. **Backend контейнер упал или перезапускается в цикле**
2. **Недостаточно ресурсов сервера** (память, CPU)
3. **Backend не успевает отвечать** (таймауты)
4. **Проблемы с подключением к БД или Redis**
5. **Ошибки в коде**, вызывающие краш приложения
6. **Неправильная конфигурация Nginx**

## 🔍 Диагностика

### 1. Проверка статуса контейнеров

```bash
docker compose ps
```

**Ожидаемый результат:**
```
NAME                STATUS
chvetyk_backend     Up (healthy)
chvetyk_postgres    Up (healthy)
chvetyk_redis       Up (healthy)
```

**Проблемы:**
- `Restarting` - контейнер постоянно перезапускается
- `Exited` - контейнер упал
- `Unhealthy` - контейнер не проходит healthcheck

### 2. Проверка логов backend

```bash
# Последние 100 строк логов
docker compose logs --tail=100 backend

# Логи в реальном времени
docker compose logs -f backend

# Только ошибки
docker compose logs backend | grep -i error
```

**Что искать:**
- Ошибки подключения к БД
- Ошибки подключения к Redis
- Ошибки при запуске
- Out of memory (OOM)
- Stack traces

### 3. Проверка использования ресурсов

```bash
# Использование памяти и CPU
docker stats

# Использование диска
df -h

# Использование памяти системой
free -h
```

**Проблемы:**
- Память > 90% - может быть недостаточно RAM
- CPU > 90% - может быть недостаточно CPU
- Диск > 90% - может быть недостаточно места

### 4. Проверка подключения к backend напрямую

```bash
# Проверка health check
curl http://localhost:3000/health

# Проверка изнутри контейнера
docker compose exec backend curl http://localhost:3000/health
```

**Ожидаемый результат:**
```json
{
  "timestamp": "2026-01-21T...",
  "database": true,
  "redis": true
}
```

### 5. Проверка конфигурации Nginx

```bash
# Проверка конфигурации Nginx
sudo nginx -t

# Просмотр конфигурации сайта
cat /etc/nginx/sites-available/bot-flowers-studio-ru.ru
```

**Важно проверить:**
- Правильный адрес backend (обычно `http://localhost:3000` или `http://127.0.0.1:3000`)
- Таймауты (должны быть достаточными)
- `proxy_read_timeout` (рекомендуется 60s или больше)

## 🛠️ Решения

### Решение 1: Перезапуск контейнеров

```bash
# Остановка всех контейнеров
docker compose down

# Запуск заново
docker compose up -d

# Проверка статуса
docker compose ps
```

### Решение 2: Увеличение ресурсов (если не хватает мощности)

#### Увеличение лимитов памяти для Docker

Создайте или отредактируйте `/etc/docker/daemon.json`:

```json
{
  "default-ulimits": {
    "memlock": {
      "hard": -1,
      "soft": -1
    }
  }
}
```

Перезапустите Docker:
```bash
sudo systemctl restart docker
```

#### Ограничение использования памяти контейнерами

Добавьте в `docker-compose.yml`:

```yaml
services:
  backend:
    # ... существующие настройки
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
```

### Решение 3: Увеличение таймаутов Nginx

Отредактируйте конфигурацию Nginx:

```nginx
server {
    # ... другие настройки
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Увеличенные таймауты
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}
```

Перезагрузите Nginx:
```bash
sudo nginx -t
sudo systemctl reload nginx
```

### Решение 4: Проверка и исправление ошибок в коде

Если backend падает из-за ошибок:

1. **Проверьте логи на ошибки:**
```bash
docker compose logs backend | grep -i "error\|exception\|fatal"
```

2. **Проверьте подключение к БД:**
```bash
docker compose exec backend npm run migrate
```

3. **Проверьте подключение к Redis:**
```bash
docker compose exec redis redis-cli ping
```

### Решение 5: Добавление healthcheck для backend

Добавьте в `docker-compose.yml`:

```yaml
services:
  backend:
    # ... существующие настройки
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
```

### Решение 6: Мониторинг и автоматический перезапуск

Создайте скрипт для мониторинга:

```bash
#!/bin/bash
# monitor-backend.sh

while true; do
    if ! curl -f http://localhost:3000/health > /dev/null 2>&1; then
        echo "$(date): Backend is down, restarting..."
        docker compose restart backend
        sleep 10
    fi
    sleep 30
done
```

Запустите как сервис systemd или через cron.

## 📊 Мониторинг производительности

### Проверка использования памяти backend

```bash
docker stats chvetyk_backend --no-stream
```

### Проверка количества запросов

```bash
# Логи Nginx
sudo tail -f /var/log/nginx/access.log | grep 502

# Логи backend
docker compose logs backend | grep -i "request\|response"
```

## 🔄 Профилактика

1. **Регулярные бэкапы БД**
2. **Мониторинг использования ресурсов**
3. **Логирование всех ошибок**
4. **Настройка алертов** (например, через Telegram бота)
5. **Регулярное обновление зависимостей**

## 📝 Чек-лист диагностики

- [ ] Проверить статус контейнеров (`docker compose ps`)
- [ ] Проверить логи backend (`docker compose logs backend`)
- [ ] Проверить использование ресурсов (`docker stats`)
- [ ] Проверить health check (`curl http://localhost:3000/health`)
- [ ] Проверить конфигурацию Nginx
- [ ] Проверить подключение к БД
- [ ] Проверить подключение к Redis
- [ ] Проверить наличие ошибок в коде

## 🆘 Если ничего не помогает

1. **Полный перезапуск:**
```bash
docker compose down -v
docker compose up -d --build
docker compose exec backend npm run migrate
```

2. **Проверка системных логов:**
```bash
sudo journalctl -u docker -n 100
sudo dmesg | tail -50
```

3. **Обращение к хостинг-провайдеру** - возможно, проблема на стороне сервера

---

**Важно:** Если проблема повторяется регулярно, это может указывать на:
- Недостаточные ресурсы сервера (нужно увеличить RAM/CPU)
- Утечки памяти в приложении
- Проблемы с базой данных (нужна оптимизация запросов)
