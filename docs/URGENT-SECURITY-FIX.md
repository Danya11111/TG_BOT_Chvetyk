# 🚨 СРОЧНО: Устранение утечки секретов

## Критическая проблема

Файл `env.txt` с реальными секретами (включая `TELEGRAM_BOT_TOKEN`) находится в Git репозитории и был закоммичен.

## Немедленные действия

### 1. СРОЧНО: Смените токен бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите "API Token"
5. Выберите "Revoke current token" (отозвать текущий токен)
6. Создайте новый токен
7. Обновите токен на сервере в переменных окружения

### 2. Удалите файл из Git истории

**ВНИМАНИЕ:** Эти команды изменят историю Git. Если репозиторий уже используется другими разработчиками, нужно будет синхронизировать изменения.

```bash
# 1. Удалить файл из индекса (но оставить локально)
git rm --cached env.txt

# 2. Закоммитить удаление
git commit -m "SECURITY: Remove secrets from repository"

# 3. Если файл уже был запушен в удаленный репозиторий:
git push origin main --force

# ВНИМАНИЕ: --force перезапишет историю на удаленном сервере
# Убедитесь, что все разработчики знают об этом
```

### 3. Очистка истории Git (опционально, но рекомендуется)

Если файл был в репозитории долгое время, лучше полностью удалить его из истории:

```bash
# Использовать git filter-branch или BFG Repo-Cleaner
# Пример с git filter-branch:
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch env.txt" \
  --prune-empty --tag-name-filter cat -- --all

# После этого:
git push origin --force --all
git push origin --force --tags
```

**Альтернатива (проще):** Использовать [BFG Repo-Cleaner](https://rtyley.github.io/bfg-repo-cleaner/):
```bash
# Скачать BFG
# Удалить файл из истории
java -jar bfg.jar --delete-files env.txt
git reflog expire --expire=now --all && git gc --prune=now --aggressive
```

### 4. Проверьте другие секреты

Проверьте, нет ли других файлов с секретами:
```bash
git log --all --full-history -- env.txt
git log --all --full-history -- "*.env"
git log --all --full-history -- "*.key"
git log --all --full-history -- "*.pem"
```

### 5. Обновите .gitignore

Файл `env.txt` уже добавлен в `.gitignore`. Убедитесь, что он там есть:
```bash
cat .gitignore | grep env.txt
```

### 6. Создайте env.example

Создайте файл `env.example` с примерами (без реальных значений):
```bash
cp env.txt env.example
# Затем отредактируйте env.example, заменив все реальные значения на примеры
```

## После исправления

1. ✅ Токен бота изменен
2. ✅ Файл удален из Git
3. ✅ История очищена (если возможно)
4. ✅ `.gitignore` обновлен
5. ✅ `env.example` создан

## Предотвращение в будущем

1. **Никогда не коммитьте файлы с секретами**
2. **Используйте переменные окружения на сервере**
3. **Используйте Docker secrets или внешние системы управления секретами**
4. **Настройте pre-commit hook для проверки:**

```bash
# .git/hooks/pre-commit
#!/bin/sh
if git diff --cached --name-only | grep -E '\.(env|key|pem)$|env\.txt'; then
  echo "ERROR: Attempting to commit sensitive files!"
  exit 1
fi
```

5. **Используйте инструменты для сканирования секретов:**
   - [git-secrets](https://github.com/awslabs/git-secrets)
   - [truffleHog](https://github.com/trufflesecurity/trufflehog)
   - [gitguardian](https://www.gitguardian.com/)

## Проверка безопасности

После исправления проверьте:
```bash
# Проверить, что файл не отслеживается
git ls-files | grep env.txt

# Должно быть пусто (нет вывода)
```

---

**ВАЖНО:** Если репозиторий публичный или был доступен другим людям, считайте все секреты скомпрометированными и замените их все!
