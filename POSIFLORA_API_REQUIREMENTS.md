# Требования к данным из Posiflora API

Документ содержит детальный список данных, методов и информации, необходимых из системы учёта Posiflora для интеграции с Telegram Bot и Mini App.

## 🔑 1. Аутентификация и доступ

### 1.1. API доступы
- [ ] **Базовый URL API** (endpoint)
  - Например: `https://api.posiflora.ru` или `https://posiflora.ru/api/v1`
  - URL для тестового окружения (если отличается)
- [ ] **Метод аутентификации**
  - API Key / Token
  - OAuth 2.0
  - Basic Auth
  - Другой метод
- [ ] **Учётные данные**
  - API Key / Token для production
  - API Key / Token для тестового окружения
  - Логин/пароль (если используется)
- [ ] **Документация по аутентификации**
  - Как получить токен
  - Как обновить токен
  - Срок действия токена
  - Headers для запросов (например: `Authorization: Bearer {token}`)

### 1.2. Лимиты и ограничения
- [ ] **Rate limits**
  - Количество запросов в минуту/час/день
  - Лимиты для разных методов (GET, POST, PUT, DELETE)
- [ ] **Размер запроса/ответа**
  - Максимальный размер запроса
  - Максимальное количество объектов в ответе
  - Пагинация (как работает, параметры)

---

## 🛍️ 2. Каталог товаров (Products/Catalog)

### 2.1. Методы API для товаров

#### 2.1.1. Получение списка товаров
- [ ] **Метод**: `GET /api/products` или аналогичный
- [ ] **Параметры запроса**:
  - Пагинация: `page`, `limit`, `offset`
  - Фильтры: `category_id`, `search`, `price_min`, `price_max`, `in_stock`
  - Сортировка: `sort_by` (price, name, popularity), `order` (asc, desc)
- [ ] **Пример ответа**:
  ```json
  {
    "data": [
      {
        "id": "string|number",
        "name": "string",
        "description": "string",
        "price": "number",
        "old_price": "number|null",
        "currency": "string (RUB)",
        "category_id": "string|number",
        "category_name": "string",
        "images": ["url1", "url2"],
        "in_stock": "boolean",
        "stock_quantity": "number|null",
        "article": "string",
        "sku": "string",
        "weight": "number|null",
        "dimensions": "object|null",
        "attributes": "object|null",
        "bonus_percent": "number|null",
        "created_at": "datetime",
        "updated_at": "datetime"
      }
    ],
    "pagination": {
      "page": "number",
      "limit": "number",
      "total": "number",
      "total_pages": "number"
    }
  }
  ```

#### 2.1.2. Получение одного товара
- [ ] **Метод**: `GET /api/products/{id}` или аналогичный
- [ ] **Параметры**: `id` (product_id)
- [ ] **Пример ответа**: тот же формат, что и элемент массива выше

#### 2.1.3. Поиск товаров
- [ ] **Метод**: `GET /api/products/search` или `GET /api/products?search=...`
- [ ] **Параметры**: `query`, `category_id`, пагинация
- [ ] **Формат ответа**: аналогично списку товаров

### 2.2. Структура данных товара

#### 2.2.1. Обязательные поля
- [ ] **id** (string|number) - уникальный идентификатор
- [ ] **name** (string) - название товара
- [ ] **price** (number) - цена в рублях
- [ ] **category_id** (string|number) - ID категории
- [ ] **in_stock** (boolean) - наличие на складе

#### 2.2.2. Опциональные поля
- [ ] **description** (string) - описание товара
- [ ] **old_price** (number|null) - старая цена (для акций)
- [ ] **currency** (string) - валюта (по умолчанию RUB)
- [ ] **images** (array[string]) - массив URL изображений
- [ ] **stock_quantity** (number|null) - количество на складе
- [ ] **article** (string) - артикул
- [ ] **sku** (string) - SKU код
- [ ] **weight** (number|null) - вес в граммах/килограммах
- [ ] **dimensions** (object) - размеры (длина, ширина, высота)
- [ ] **attributes** (object) - дополнительные атрибуты (цвет, размер и т.д.)
- [ ] **bonus_percent** (number|null) - процент бонусов за покупку
- [ ] **created_at** (datetime) - дата создания
- [ ] **updated_at** (datetime) - дата обновления

### 2.3. Категории товаров

#### 2.3.1. Получение категорий
- [ ] **Метод**: `GET /api/categories` или аналогичный
- [ ] **Параметры**:
  - `parent_id` (для иерархии)
  - `include_products` (boolean - включать ли товары)
- [ ] **Пример ответа**:
  ```json
  {
    "data": [
      {
        "id": "string|number",
        "name": "string",
        "slug": "string",
        "parent_id": "string|number|null",
        "description": "string|null",
        "image": "string|null",
        "icon": "string|null",
        "sort_order": "number",
        "is_active": "boolean",
        "products_count": "number",
        "children": ["array of categories if hierarchical"]
      }
    ]
  }
  ```

#### 2.3.2. Структура категории
- [ ] **id** (string|number) - ID категории
- [ ] **name** (string) - название
- [ ] **slug** (string) - URL-friendly название
- [ ] **parent_id** (string|number|null) - родительская категория
- [ ] **description** (string|null) - описание
- [ ] **image** (string|null) - URL изображения категории
- [ ] **icon** (string|null) - иконка категории
- [ ] **sort_order** (number) - порядок сортировки
- [ ] **is_active** (boolean) - активна ли категория
- [ ] **products_count** (number) - количество товаров в категории

### 2.4. Изображения товаров
- [ ] **Формат URL изображений**
  - Полный URL: `https://posiflora.ru/images/product.jpg`
  - Относительный путь: `/images/product.jpg`
  - CDN URL: `https://cdn.posiflora.ru/...`
- [ ] **Размеры изображений**
  - Оригинал: размер
  - Миниатюра (thumbnail): размер
  - Средний размер: размер
- [ ] **Поддержка нескольких изображений**
  - Максимальное количество изображений
  - Порядок изображений

---

## 🛒 3. Заказы (Orders)

### 3.1. Создание заказа

#### 3.1.1. Метод создания заказа
- [ ] **Метод**: `POST /api/orders` или аналогичный
- [ ] **Параметры запроса**:
  ```json
  {
    "customer": {
      "name": "string",
      "phone": "string",
      "email": "string|null",
      "telegram_id": "string|number",
      "telegram_username": "string|null"
    },
    "items": [
      {
        "product_id": "string|number",
        "quantity": "number",
        "price": "number",
        "name": "string"
      }
    ],
    "delivery": {
      "type": "delivery|pickup",
      "address": {
        "city": "string",
        "street": "string",
        "house": "string",
        "apartment": "string|null",
        "postal_code": "string|null",
        "coordinates": {
          "lat": "number|null",
          "lng": "number|null"
        }
      },
      "pickup_point_id": "string|number|null",
      "delivery_date": "date|null",
      "delivery_time": "time|null",
      "delivery_cost": "number"
    },
    "payment": {
      "type": "cash|card|online|transfer",
      "status": "pending|paid"
    },
    "bonuses": {
      "used": "number",
      "to_be_accrued": "number"
    },
    "comment": "string|null",
    "source": "telegram_bot",
    "total": "number",
    "subtotal": "number",
    "delivery_cost": "number",
    "bonus_discount": "number"
  }
  ```

#### 3.1.2. Обязательные поля при создании
- [ ] **customer.phone** - обязательно
- [ ] **customer.name** - обязательно
- [ ] **items** - массив товаров (минимум 1 товар)
- [ ] **delivery.type** - обязательно
- [ ] **delivery.address** - обязательно для доставки
- [ ] **delivery.pickup_point_id** - обязательно для самовывоза
- [ ] **payment.type** - обязательно
- [ ] **total** - итоговая сумма

#### 3.1.3. Валидация при создании
- [ ] **Минимальная сумма заказа** - какая сумма?
- [ ] **Максимальное количество одного товара** - есть ли ограничение?
- [ ] **Проверка наличия товаров** - проверяется ли автоматически?
- [ ] **Проверка цены** - используется ли текущая цена из каталога или цена из запроса?

#### 3.1.4. Ответ при создании заказа
- [ ] **Формат ответа**:
  ```json
  {
    "success": "boolean",
    "data": {
      "id": "string|number",
      "number": "string",
      "status": "string",
      "status_code": "string|number",
      "created_at": "datetime",
      "total": "number",
      "payment_url": "string|null",
      "message": "string|null"
    },
    "errors": ["array of errors if failed"]
  }
  ```

### 3.2. Получение информации о заказе

#### 3.2.1. Метод получения заказа
- [ ] **Метод**: `GET /api/orders/{id}` или аналогичный
- [ ] **Параметры**: `id` (order_id)
- [ ] **Формат ответа**:
  ```json
  {
    "id": "string|number",
    "number": "string",
    "status": "string",
    "status_code": "string|number",
    "status_name": "string",
    "status_description": "string|null",
    "customer": {
      "id": "string|number",
      "name": "string",
      "phone": "string",
      "email": "string|null"
    },
    "items": [
      {
        "id": "string|number",
        "product_id": "string|number",
        "product_name": "string",
        "quantity": "number",
        "price": "number",
        "total": "number",
        "image": "string|null"
      }
    ],
    "delivery": {
      "type": "delivery|pickup",
      "address": "object",
      "pickup_point": "object|null",
      "delivery_date": "date|null",
      "delivery_time": "time|null",
      "delivery_cost": "number"
    },
    "payment": {
      "type": "string",
      "status": "string"
    },
    "bonuses": {
      "used": "number",
      "accrued": "number",
      "to_be_accrued": "number"
    },
    "totals": {
      "subtotal": "number",
      "delivery_cost": "number",
      "bonus_discount": "number",
      "total": "number"
    },
    "comment": "string|null",
    "created_at": "datetime",
    "updated_at": "datetime",
    "status_history": [
      {
        "status": "string",
        "status_code": "string|number",
        "changed_at": "datetime",
        "comment": "string|null"
      }
    ]
  }
  ```

### 3.3. Список заказов пользователя

#### 3.3.1. Метод получения заказов
- [ ] **Метод**: `GET /api/orders` или `GET /api/customers/{customer_id}/orders`
- [ ] **Параметры**:
  - `customer_id` или `telegram_id` или `phone` - для фильтрации по пользователю
  - `status` - фильтр по статусу
  - `date_from`, `date_to` - фильтр по дате
  - Пагинация
- [ ] **Формат ответа**: массив заказов (упрощённая версия) или полная версия

### 3.4. Статусы заказов

#### 3.4.1. Список статусов
- [ ] **Возможные статусы**:
  - `new` / `pending` - Новый заказ
  - `confirmed` - Подтверждён
  - `preparing` / `processing` - В обработке / В сборке
  - `ready` - Готов к выдаче
  - `shipped` / `in_delivery` - Отправлен / В доставке
  - `delivered` / `completed` - Доставлен / Выполнен
  - `cancelled` - Отменён
  - `refunded` - Возврат
  - Другие статусы (уточнить)

#### 3.4.2. Коды статусов
- [ ] **Числовые коды статусов** (если используются)
- [ ] **Соответствие статусов и кодов**

#### 3.4.3. Описания статусов
- [ ] **Название статуса** (человекочитаемое)
- [ ] **Описание статуса** (что означает)
- [ ] **Иконки статусов** (если есть)

### 3.5. История изменения статусов

#### 3.5.1. Метод получения истории
- [ ] **Метод**: `GET /api/orders/{id}/status-history` или входит в заказ
- [ ] **Формат**: массив изменений статусов с датами

### 3.6. Обновление статуса заказа

#### 3.6.1. Метод обновления (если доступен извне)
- [ ] **Метод**: `PUT /api/orders/{id}/status` или `PATCH /api/orders/{id}`
- [ ] **Параметры**: `status`, `comment`
- [ ] **Ограничения**: какие статусы можно устанавливать из API

---

## 💰 4. Бонусная система (Bonus System)

### 4.1. Получение баланса бонусов пользователя

#### 4.1.1. Метод получения баланса
- [ ] **Метод**: `GET /api/customers/{customer_id}/bonuses` или `GET /api/bonuses?customer_id=...`
- [ ] **Параметры**: `customer_id` или `phone` или `telegram_id`
- [ ] **Формат ответа**:
  ```json
  {
    "customer_id": "string|number",
    "total_bonuses": "number",
    "available_bonuses": "number",
    "pending_bonuses": "number",
    "used_bonuses": "number",
    "currency": "RUB",
    "last_updated": "datetime"
  }
  ```

### 4.2. Расчёт начисляемых бонусов

#### 4.2.1. Метод расчёта бонусов за заказ
- [ ] **Метод**: `POST /api/bonuses/calculate` или `GET /api/bonuses/calculate?items=...`
- [ ] **Параметры**:
  ```json
  {
    "customer_id": "string|number|null",
    "items": [
      {
        "product_id": "string|number",
        "quantity": "number",
        "price": "number"
      }
    ],
    "total": "number",
    "use_existing_bonuses": "number|null"
  }
  ```
- [ ] **Формат ответа**:
  ```json
  {
    "bonuses_to_accrue": "number",
    "bonus_percent": "number",
    "can_use_bonuses": "boolean",
    "max_bonuses_to_use": "number",
    "discount_from_bonuses": "number",
    "final_total": "number",
    "calculation_rules": {
      "accrual_rule": "string",
      "usage_rule": "string",
      "min_order_amount": "number",
      "max_bonus_percent": "number"
    }
  }
  ```

### 4.3. Правила начисления бонусов

#### 4.3.1. Получение правил
- [ ] **Метод**: `GET /api/bonuses/rules` или аналогичный
- [ ] **Информация о правилах**:
  - Процент начисления (по умолчанию и для категорий)
  - Минимальная сумма заказа для начисления
  - Максимальный процент от суммы заказа
  - Категории товаров с повышенным процентом
  - Акции и специальные предложения

### 4.4. Правила списания бонусов

#### 4.4.1. Информация о списании
- [ ] **Можно ли списывать бонусы** (да/нет)
- [ ] **Максимальный процент списания** (например, 50% от суммы)
- [ ] **Минимальная сумма заказа** для списания
- [ ] **Шаг списания** (можно ли списать любое количество или кратно чему-то)

### 4.5. История операций с бонусами

#### 4.5.1. Метод получения истории
- [ ] **Метод**: `GET /api/customers/{customer_id}/bonuses/history`
- [ ] **Параметры**: пагинация, фильтры по типу операции
- [ ] **Формат ответа**:
  ```json
  {
    "data": [
      {
        "id": "string|number",
        "type": "accrued|used|expired|cancelled",
        "amount": "number",
        "order_id": "string|number|null",
        "description": "string",
        "created_at": "datetime"
      }
    ]
  }
  ```

---

## 👤 5. Клиенты (Customers/Users)

### 5.1. Получение информации о клиенте

#### 5.1.1. Метод получения клиента
- [ ] **Метод**: `GET /api/customers/{id}` или `GET /api/customers?phone=...&telegram_id=...`
- [ ] **Параметры поиска**: `id`, `phone`, `telegram_id`, `email`
- [ ] **Формат ответа**:
  ```json
  {
    "id": "string|number",
    "name": "string",
    "phone": "string",
    "email": "string|null",
    "telegram_id": "string|number|null",
    "telegram_username": "string|null",
    "addresses": [
      {
        "id": "string|number",
        "type": "delivery|billing",
        "city": "string",
        "street": "string",
        "house": "string",
        "apartment": "string|null",
        "postal_code": "string|null",
        "is_default": "boolean"
      }
    ],
    "bonus_balance": "number",
    "orders_count": "number",
    "total_spent": "number",
    "created_at": "datetime",
    "updated_at": "datetime"
  }
  ```

### 5.2. Создание/обновление клиента

#### 5.2.1. Метод создания клиента
- [ ] **Метод**: `POST /api/customers`
- [ ] **Параметры**:
  ```json
  {
    "name": "string",
    "phone": "string",
    "email": "string|null",
    "telegram_id": "string|number",
    "telegram_username": "string|null"
  }
  ```

#### 5.2.2. Метод обновления клиента
- [ ] **Метод**: `PUT /api/customers/{id}` или `PATCH /api/customers/{id}`
- [ ] **Параметры**: те же, что и при создании

### 5.3. Поиск клиента по телефону/Telegram ID

#### 5.3.1. Метод поиска
- [ ] **Метод**: `GET /api/customers/search` или `GET /api/customers?phone=...`
- [ ] **Параметры**: `phone`, `telegram_id`, `email`
- [ ] **Формат ответа**: массив найденных клиентов или один клиент

---

## 🚚 6. Доставка и точки самовывоза

### 6.1. Расчёт стоимости доставки

#### 6.1.1. Метод расчёта доставки
- [ ] **Метод**: `POST /api/delivery/calculate` или `GET /api/delivery/calculate?address=...`
- [ ] **Параметры**:
  ```json
  {
    "address": {
      "city": "string",
      "street": "string",
      "house": "string",
      "apartment": "string|null",
      "postal_code": "string|null",
      "coordinates": {
        "lat": "number",
        "lng": "number"
      }
    },
    "order_total": "number",
    "items": [
      {
        "product_id": "string|number",
        "quantity": "number",
        "weight": "number|null"
      }
    ],
    "delivery_date": "date|null",
    "delivery_time": "time|null"
  }
  ```
- [ ] **Формат ответа**:
  ```json
  {
    "delivery_cost": "number",
    "free_delivery_threshold": "number|null",
    "is_free_delivery": "boolean",
    "estimated_delivery_date": "date",
    "estimated_delivery_time": "time",
    "delivery_zones": [
      {
        "zone_id": "string|number",
        "zone_name": "string",
        "cost": "number",
        "delivery_time": "string"
      }
    ],
    "available_time_slots": [
      {
        "date": "date",
        "time_slots": ["10:00-12:00", "14:00-16:00", ...]
      }
    ]
  }
  ```

### 6.2. Точки самовывоза

#### 6.2.1. Метод получения точек самовывоза
- [ ] **Метод**: `GET /api/pickup-points` или `GET /api/delivery/pickup-points`
- [ ] **Параметры**: `city`, `is_active`
- [ ] **Формат ответа**:
  ```json
  {
    "data": [
      {
        "id": "string|number",
        "name": "string",
        "address": {
          "city": "string",
          "street": "string",
          "house": "string",
          "postal_code": "string|null",
          "coordinates": {
            "lat": "number",
            "lng": "number"
          }
        },
        "working_hours": {
          "monday": "09:00-18:00",
          "tuesday": "09:00-18:00",
          ...
        },
        "phone": "string|null",
        "description": "string|null",
        "image": "string|null",
        "map_url": "string|null",
        "is_active": "boolean",
        "sort_order": "number"
      }
    ]
  }
  ```

### 6.3. Зоны доставки

#### 6.3.1. Метод получения зон доставки
- [ ] **Метод**: `GET /api/delivery/zones`
- [ ] **Параметры**: `city`
- [ ] **Формат ответа**: список зон доставки с ценами и условиями

---

## 📢 7. Webhooks (если поддерживаются)

### 7.1. Настройка webhooks

#### 7.1.1. Поддержка webhooks
- [ ] **Поддерживает ли Posiflora webhooks?** (да/нет)
- [ ] **Метод настройки**: через API или через админ-панель
- [ ] **Метод регистрации webhook**: `POST /api/webhooks` или аналогичный

#### 7.1.2. События для webhooks
- [ ] **order.created** - создан новый заказ
- [ ] **order.status_changed** - изменился статус заказа
- [ ] **order.cancelled** - заказ отменён
- [ ] **order.completed** - заказ выполнен
- [ ] **product.updated** - обновлён товар
- [ ] **product.stock_changed** - изменилось количество товара
- [ ] Другие события

#### 7.1.3. Формат webhook запроса
- [ ] **URL для webhook** (куда Posiflora отправляет данные)
- [ ] **Метод запроса** (POST)
- [ ] **Headers** (авторизация, подпись запроса)
- [ ] **Формат тела запроса** (JSON)
- [ ] **Подпись запроса** (для проверки подлинности)

### 7.2. Пример webhook payload

#### 7.2.1. Событие изменения статуса заказа
```json
{
  "event": "order.status_changed",
  "timestamp": "2024-01-10T12:00:00Z",
  "data": {
    "order_id": "string|number",
    "order_number": "string",
    "old_status": "string",
    "new_status": "string",
    "status_code": "string|number",
    "customer_id": "string|number",
    "customer_phone": "string",
    "total": "number"
  },
  "signature": "string (для проверки)"
}
```

---

## 🔄 8. Синхронизация данных

### 8.1. Получение изменений

#### 8.1.1. Метод получения изменений товаров
- [ ] **Метод**: `GET /api/products/changes` или аналогичный
- [ ] **Параметры**: `since` (дата последней синхронизации), `updated_at`
- [ ] **Формат**: список изменённых товаров

#### 8.1.2. Полноценная синхронизация каталога
- [ ] **Метод полной синхронизации**: `GET /api/products?sync=true`
- [ ] **Как часто нужно синхронизировать?**
- [ ] **Есть ли метод массовой загрузки?**

### 8.2. Кэширование данных

#### 8.2.1. Рекомендации по кэшированию
- [ ] **TTL для каталога товаров** (сколько можно кэшировать)
- [ ] **TTL для цен** (как часто обновляются)
- [ ] **TTL для наличия** (актуальность данных о наличии)

---

## ⚠️ 9. Обработка ошибок

### 9.1. Формат ошибок API

#### 9.1.1. Стандартный формат ошибок
- [ ] **Формат ответа при ошибке**:
  ```json
  {
    "success": false,
    "error": {
      "code": "string|number",
      "message": "string",
      "details": "object|null",
      "field_errors": {
        "field_name": ["error message"]
      }
    }
  }
  ```

#### 9.1.2. Коды ошибок
- [ ] **400** - Неверный запрос
- [ ] **401** - Не авторизован
- [ ] **403** - Доступ запрещён
- [ ] **404** - Не найдено
- [ ] **422** - Ошибка валидации
- [ ] **429** - Превышен лимит запросов
- [ ] **500** - Ошибка сервера
- [ ] Другие коды

### 9.2. Частые ошибки

#### 9.2.1. При создании заказа
- [ ] Товар не найден
- [ ] Товар отсутствует на складе
- [ ] Неверная цена товара
- [ ] Неверные данные клиента
- [ ] Неверный адрес доставки
- [ ] Ошибка при начислении/списании бонусов

---

## 🧪 10. Тестовое окружение

### 10.1. Тестовый API

#### 10.1.1. Тестовый доступ
- [ ] **URL тестового API**: отличается ли от production?
- [ ] **Тестовые данные**: есть ли тестовые товары, заказы?
- [ ] **Тестовые учётные данные**: отдельный API ключ для тестов?

### 10.2. Тестовые сценарии

#### 10.2.1. Проверка интеграции
- [ ] Создание тестового заказа
- [ ] Проверка расчёта доставки
- [ ] Проверка расчёта бонусов
- [ ] Проверка получения статусов

---

## 📝 11. Дополнительная информация

### 11.1. Документация
- [ ] **Полная документация API** (PDF, онлайн, Swagger/OpenAPI)
- [ ] **Примеры запросов** (curl, Postman collection)
- [ ] **Человекочитаемые описания** методов
- [ ] **Диаграммы** процессов (создание заказа, начисление бонусов)

### 11.2. Поддержка
- [ ] **Контакты технической поддержки** Posiflora
- [ ] **Время работы поддержки**
- [ ] **Каналы связи** (email, телефон, чат)
- [ ] **Средний срок ответа** на вопросы

### 11.3. Обновления API
- [ ] **Версионирование API** (есть ли версии v1, v2?)
- [ ] **План обновлений** (когда планируются изменения)
- [ ] **Deprecation policy** (как долго поддерживаются старые версии)
- [ ] **Уведомления об изменениях** (email, webhook, документация)

---

## ✅ Чеклист готовности к интеграции

### Обязательно для начала разработки:
- [x] Базовый URL API
- [x] Метод аутентификации
- [x] API ключ (токен)
- [x] Получение списка товаров
- [x] Получение категорий товаров
- [x] Создание заказа
- [x] Получение статуса заказа
- [x] Получение баланса бонусов
- [x] Расчёт начисляемых бонусов
- [x] Расчёт стоимости доставки
- [x] Получение точек самовывоза
- [x] Список статусов заказов
- [x] Формат ошибок API

### Желательно для полной функциональности:
- [ ] Webhooks для уведомлений о статусах
- [ ] История операций с бонусами
- [ ] История изменений статусов
- [ ] Поиск клиентов
- [ ] Создание/обновление клиентов
- [ ] Синхронизация каталога (delta updates)
- [ ] Тестовое окружение
- [ ] Полная документация API

---

## 📌 Примечания

### Важно уточнить:
1. **Версия API** - какая версия используется (v1, v2)?
2. **Формат дат** - ISO 8601, Unix timestamp, другой формат?
3. **Формат денежных значений** - число или строка? В копейках или рублях?
4. **Часовой пояс** - какой часовой пояс используется в API?
5. **Кодировка** - UTF-8, Windows-1251?
6. **Формат изображений** - какие форматы поддерживаются (jpg, png, webp)?

### Рекомендации:
- Запросить **Postman collection** или **Swagger/OpenAPI** документацию
- Запросить **тестовый доступ** для разработки
- Установить **дедлайн** для предоставления документации
- Создать **канал связи** с технической поддержкой Posiflora
